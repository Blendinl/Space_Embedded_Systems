enum State {
    normal_operation,
    backup_operation,
}


fn main() {
    let mut current_state: State = normal_operation;
    let mut (current_position, stepper_motor) = (-1, -1);
    loop {
    
    }
    println!("Hello, world!");
}
